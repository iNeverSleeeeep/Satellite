# 架构说明

## 建议的仿真分层

1. 任务层：轨道工况、任务场景和仿真流程编排
2. 对象层：刚体动力学、传感器和执行机构模型
3. GNC 层：制导、导航、状态估计和控制算法
4. 环境层：重力、气动阻力、太阳辐射压和外部扰动
5. 分析层：数据记录、指标提取、绘图与结果验证

## 建模约定

- 可复用的模块尽量放在 `models/libraries/`
- 参数和初始化逻辑尽量不要直接写死在 `.slx` 文件里
- 仿真生成的输出结果统一放到 `results/`
